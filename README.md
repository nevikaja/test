# Test Repository

This repository contains a simple example website with a basic calculator.

The site is located in the `website` directory and can be opened directly in a browser. Enter two numbers, choose an operation, and view the result.
